<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
<style>
    body{
        margin: 0;
        padding: 0;
        outline: 0;
        box-sizing: border-box;
    }
    .container{
        background-color: #395457;
        padding: 10px;
    }
    .item-1{
        display: flex;
        justify-content: space-between;
        align-items: center;
        border: 4px solid #6c8e91;
        margin-bottom: 10px;
        background-color: ;
    }
    h2{
        padding-left: 10px;
        color: #fff;
        text-transform: uppercase;
    }
    .item-1 button{
        margin-right: 10px;
        width: 100px;
        padding: 8px;
        background-color: #0b8fd6;
        border: none;
        cursor: pointer;
        color: #fff;
        border-radius: 5px;
        text-transform: uppercase;
    }
    .item-3{
        border: 2px solid #6c8e91;
        margin-bottom: 5px;
        display: flex;
        justify-content: space-between;
    }
    .btn{
        margin-right: 10px;
    }
    .item-btn{
        width: 100px;
        padding: 8px;
        border: none;
        background-color: #0b8fd6;
        margin: 10px 0;
        cursor: pointer;
        color: #fff;
        border-radius: 5px;
        text-transform: uppercase;
    }
    h3{
        margin-left: 10px;
        color: #fff;
    }
    .item-4{
        text-align: center;
    }
</style>
</head>
<body>
    <div class="container">
        <div class="item-1">
            <h2>Friends information</h2>
            <a href="/add"><button>Create</button></a>
        </div>
        <div class="item-2">
            {{#if posts}}
            {{#each posts}}
                <div class="item-3"> 
                  <div>
                    <h3>Name: {{name}}</h3>
                    <h3>Phone Number: {{phone}}</h3>
                  </div>
                   <div class="btn">
                        <a href="/edit/{{_id}}" ><button class="item-btn">Edit</button></a>
                        <form action="/delete/{{_id}}?_method=DELETE" method="POST">
                            <button class="item-btn">Delete</button>
                        </form>
                   </div>
                </div>
            {{/each}}
            {{else}}
            <div class=" item-4">
                <h1>No friends information found !</h1>
            </div>
            {{/if}}
        </div>
    </div>
</body>
</html>